#KEYWORDS:

para 
mientras
segun_sea
salir 
si 
sino si
sino 
caso
defecto
operator
extern
def
const 
super
self
retorna
intenta
atrapa
tirar
finalmente
yield
salir
fallthrough
defer
continuar
del
from
import
with
pass
void
type
override
abstract
assert

#CONSTANTES:

 falso 
 verdadero
 nulo
 infinito
 FALSO 
 VERDADERO 
 NULO 
 INFINITO 

#OPERADORES:

< : , as , > + - -- ++ * / = == != ! % ^  <<< << >> >>> xor ~ y_es o_es no es .. ... -> +=
-= %= ^= <= >= @ en no en

#TYPES:

any Entero Entero8 Entero16 Entero32 Entero64 Entero128
Sin_signo Sin_signo8 Sin_signo16 Sin_signo32 Sin_signo64 Sin_signo128
doble flotante complejo cadena booleano lista tupla conjunto dict Enum fun Estructura clase

#APERTURAS Y CIERRES:

() [] {}

(
    indentificador
)

#look and read:

mostrar()
leer

si (i=0 i<0 ++i){
     
}

mostrar("hola")